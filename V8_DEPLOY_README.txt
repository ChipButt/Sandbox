ASTRA v8 public deploy

This is the public app build.
It does not include the editor.
To change the map UI layout or map pin/box positions later, use the separate working editor ZIP and replace only:
data/map-config.js

The QR folder and printable scan target images are not included.
